'use client'
import { useState } from 'react'
export default function PromotionManager({business}:{business:any}){const [busy,setBusy]=useState(false);async function save(f:FormData){setBusy(true);const res=await fetch(`/api/business/${business.id}/promotions`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),description:f.get('description'),discount:Number(f.get('discount'))||undefined,discountType:f.get('discountType'),productId:f.get('productId')||undefined})});setBusy(false);if(!res.ok)return alert('No fue posible crear la promoción.');location.reload()}return <div className="promotion-manager"><form action={save}><input name="name" placeholder="Nombre de la promoción" required/><input name="discount" type="number" min="0" placeholder="Descuento"/><select name="discountType"><option value="PERCENTAGE">Porcentaje</option><option value="FIXED">Valor fijo</option></select><select name="productId"><option value="">Todo el menú</option>{business.products.map((p:any)=><option key={p.id} value={p.id}>{p.name}</option>)}</select><textarea name="description" placeholder="Condiciones o descripción"/><button disabled={busy} className="button-lime">{busy?'Guardando…':'Crear promoción →'}</button></form><div>{business.promotions.map((p:any)=><article key={p.id}><small>{p.isActive?'ACTIVA':'INACTIVA'}</small><h2>{p.name}</h2><p>{p.description}</p><strong>{p.discount?`${p.discount}${p.discountType==='PERCENTAGE'?'%':' COP'} OFF`:'DESTACADA'}</strong></article>)}</div></div>}
