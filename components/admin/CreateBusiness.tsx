'use client'
import { useState } from 'react'
export default function CreateBusiness({owners,plans}:{owners:any[],plans:any[]}){const [open,setOpen]=useState(false);const [busy,setBusy]=useState(false);async function create(f:FormData){setBusy(true);const response=await fetch('/api/admin/businesses',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),slug:f.get('slug'),ownerId:f.get('ownerId'),planId:f.get('planId')||undefined})});setBusy(false);if(!response.ok)return alert('Revisa los datos: el slug debe ser único y el propietario válido.');location.reload()}return <><button className="button-lime" onClick={()=>setOpen(true)}>Nuevo ecosistema →</button>{open&&<div className="catalog-modal"><form action={create}><button type="button" onClick={()=>setOpen(false)}>Cerrar ×</button><p>ALTA DE NEGOCIO</p><input name="name" placeholder="Nombre del negocio" required/><input name="slug" placeholder="slug-ejemplo" pattern="[a-z0-9-]+" required/><select name="ownerId" required><option value="">Selecciona propietario</option>{owners.map(o=><option key={o.id} value={o.id}>{o.name||o.email}</option>)}</select><select name="planId"><option value="">Sin plan por ahora</option>{plans.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><button disabled={busy} className="button-lime">{busy?'Creando…':'Crear tenant →'}</button></form></div>}</>}
